# ============================================================
# anonym.in – Umgebungsvariablen
# Kopiere diese Datei als .env.local und fülle alle Werte aus.
# NIEMALS .env.local in Git committen!
# ============================================================

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# --- NextAuth ---
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=dein-geheimes-secret-mindestens-32-zeichen

# --- Resend (E-Mail) ---
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@anonym.in

# --- Stripe (Payments) ---
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# --- Admin API ---
# Schützt POST /api/admin/organizations (Authorization: Bearer ...)
ADMIN_SECRET_KEY=ein-langes-zufaelliges-geheimnis-fuer-den-admin

# --- Cron-Job ---
# Schützt GET /api/cron/deadline-reminders (Authorization: Bearer ...)
# Vercel setzt diesen Header automatisch, wenn in vercel.json konfiguriert.
CRON_SECRET=ein-anderes-langes-zufaelliges-geheimnis

# --- Encryption Keys pro Organisation ---
# Format: ORG_KEY_{UUID ohne Bindestriche}={64 Hex-Zeichen}
# Wird beim Anlegen einer Organisation über POST /api/admin/organizations
# generiert und EINMALIG zurückgegeben. Muss sofort als Env-Var gespeichert werden!
# Beispiel:
# ORG_KEY_550e8400e29b41d4a716446655440000=a3f8b2c1d4e5f6...
